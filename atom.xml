<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://shirley-wbw.github.io</id>
    <title>My Gridea</title>
    <updated>2019-07-10T08:30:32.039Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://shirley-wbw.github.io"/>
    <link rel="self" href="https://shirley-wbw.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://shirley-wbw.github.io/images/avatar.png</logo>
    <icon>https://shirley-wbw.github.io/favicon.ico</icon>
    <rights>All rights reserved 2019, My Gridea</rights>
    <entry>
        <title type="html"><![CDATA[libsvm二分类实践总结]]></title>
        <id>https://shirley-wbw.github.io/post/libsvm-er-fen-lei-shi-jian-zong-jie-linux-shang-python-li-pei-zhi</id>
        <link href="https://shirley-wbw.github.io/post/libsvm-er-fen-lei-shi-jian-zong-jie-linux-shang-python-li-pei-zhi">
        </link>
        <updated>2019-07-10T06:24:39.000Z</updated>
        <summary type="html"><![CDATA[<p>在做SVM实际应用时，检索资料大多都推荐了libsvm，这是台湾大学教授林智仁等开发的一个包，查看了很多教程，大多表述模糊，经过自己动手实践摸索，终于有一点小体悟，分享并记录。</p>
]]></summary>
        <content type="html"><![CDATA[<p>在做SVM实际应用时，检索资料大多都推荐了libsvm，这是台湾大学教授林智仁等开发的一个包，查看了很多教程，大多表述模糊，经过自己动手实践摸索，终于有一点小体悟，分享并记录。</p>
<!-- more -->
<ul>
<li>软件安装</li>
</ul>
<p>libsvm实际上是一个linux工具，但其开发者也做了python包，本文主要讲述在linux上的python里配置并使用libsvm。<a href="https://www.csie.ntu.edu.tw/~cjlin/libsvm/">libsvm官方网站</a>，下载tar.gz压缩包文件并解压<code>tar -zxvf *.tar.gz</code>，进入解压后目录<code>make</code>，进入解压后目录的python子目录中<code>make</code>，此时将解压目录下生成了libsvm.so.2，将其拷贝至自己python环境下的/lib/python3.6目录下，python子目录下的各*.py文件拷贝至自己python环境下的lib/python3.6/site-packages目录下。此时进入jupyter notebook运行以下代码，无报错说明安装成功。liblinear安装方法同libsvm安装方法基本一致。</p>
<pre><code>import svmutil
from svmutil import *
</code></pre>
<ul>
<li>libsvm使用</li>
</ul>
<p>如果不是一线搞算法研发的人员，有时候对于机器学习的算法原理其实掌握得并不是很透彻，工作中学习和了解一个算法，我们更想知道的是：该算法适用什么问题？如何应用？用哪个工具或包可以实现其应用？现在主要回答后两个问题。</p>
<ol>
<li>首先libsvm需要输入什么数据：输入数据矩阵，每行代表一个样本，第一列为label，本文做二分类，此时第一列值可以为0和1或者为-1和+1，之后的每列数据为一个特征，每个特征需要有一个index。数据格式如下：</li>
</ol>
<p>[label] [index1]:[value1] [index2]:[value2] ...</p>
<p>以下python代码可以把txt输入格式文件
[label] [value1] [value2]转换为[label] [index1]:[value1] [index2]:[value2]格式</p>
<pre><code>readin = open('home/user/input_feature.txt', 'r')
#write data file
output = open('home/user/revise_feature.txt', 'r')
try:
    the_line = readin.readline()
    while the_line:
        # delete the \n
        the_line = the_line.strip('\n')
        index = 0;
        output_line = ''
        for sub_line in the_line.split('\t'):
            #the label col
            if index == 0:
                output_line = sub_line
            #the features cols
            if sub_line != 'NULL' and index != 0:
                the_text = ' ' + str(index) + ':' + sub_line
                output_line = output_line + the_text
            index = index + 1
        output_line = output_line + '\n'
        output.write(output_line)
        the_line = readin.readline()
finally:
    readin.close()
</code></pre>
<ol start="2">
<li>获得合适格式的文件之后，以下代码可以建立svm分类器并输出其判别准确率。我们可以将数据集分为训练集和测试集，训练集建模并进行自判，测试集用模型判别。</li>
</ol>
<pre><code>y,x=svm_read_problem('home/user/revise_feature.txt')
m = svm_train(y, x, '-c 0.5 -g 0.0078125')
p_label, p_acc, p_val = svm_predict(y,x, m)
</code></pre>
<pre><code>test_y,test_x =svm_read_problem('home/user/test_feature.txt')
p_label, p_acc, p_val = svm_predict(test_y,test_x, m)
</code></pre>
<ol start="3">
<li>
<p>libsvm建模优化
用以上方法做出的模型分类效果不佳，这时可以考虑建模优化问题。libsvm有以下几个可以考虑的地方。</p>
<ol>
<li>svm-scale：功能是将所有特征默认normalization到[-1,1]范围，有参数可指定上下限。-s参数可将其缩放规则存在文件中。-r参数可指定按某种缩放规则缩放本次输入文件，实际应用中可将训练集的缩放规则用于测试集缩放。经normalization后的数据再用来建模。</li>
</ol>
<p><code>svm-scale -l -10 -u 10 -s trian.range input.txt &gt; out.txt</code></p>
<ol start="2">
<li>grid.py：在libsvm的tools目录下，有grid.py工具，功能是通过网格搜索寻找最佳的svm训练超参数c值和g值。输出结果会在终端显示，如果终端有X11转发功能，还会实时显示图片，经过网格搜索会给出最佳c值和g值，用于svm训练。但该方法并不能保证找出的c值和g值是合理的，还需判断。</li>
</ol>
<p><code>python grid.py input.txt</code></p>
</li>
</ol>
<ul>
<li>libsvm一些参数解释
libsvm在训练时也可以直接使用命令行语句，而无需进入jupyter notebook。
<code>svmtrain [options] input [model]</code></li>
</ul>
<p>-s svm类型：SVM设置类型(默认0)
0 -- C-SVC：C-支持向量分类机；参数C为惩罚系数，C越大表示对错误分类的惩罚越大，适当的参数C对分类Accuracy很关键。
1 --v-SVC：v-支持向量分类机；由于C的选取比较困难，用另一个参数v代替C。C是“无意义”的，v是有意义的。（与C_SVC其实采用的模型相同，但是它们的参数C的范围不同,C_SVC采用的是0到正无穷，该类型是[0,1]。）
2 – 一类SVM：单类别-支持向量机，不需要类标号,用于支持向量的密度估计和聚类。
3 -- e -SVR：ε-支持向量回归机，不敏感损失函数，对样本点来说，存在着一个不为目标函数提供任何损失值的区域。
4 -- v-SVR：n-支持向量回归机，由于EPSILON_SVR需要事先确定参数，然而在某些情况下选择合适的参数却不是一件容易的事情。而NU_SVR能够自动计算参数。
显然，后两者是针对回归问题的，分类问题与回归问题最大的不同就是label，分类的label是类别，比如+1，-1，回归的label是目标值，可能为任意值。</p>
<ul>
<li>总结</li>
</ul>
<p>以上是一些基本用法以及不全面的总结，遗憾的是在我的测试数据上libsvm的效果不佳，经过normalization以及网格搜索最佳超参数后效果仍然较差，有待进一步摸索和学习。</p>
<ul>
<li>背景知识补充</li>
</ul>
<p>在做测试前也检索了不少资料。摘抄一部分资料，主要是关于libsvm到底适用于解决什么样的问题。</p>
<p>libsvm用来解决通用典型的分类问题，liblinear适用于大规模数据的线性模型和高维稀疏特征问题。</p>
<p>非线性分类不一定比线性分类器好，尤其是在样本极其有限而特征维度很高的情况下，因为这是kernel map通常不准确，很可能错误划分类别空间，乃至比线性分类效果更差。</p>
<p>有几点经验性的东西：</p>
<ol>
<li>如果特征远远大于样本数，使用线性核就可以。</li>
<li>特征数和样本数都很大，如文档分类，一般使用线性核。</li>
<li>特征数远小于样本数，一般用RBF，若一定要用线性核，用liblinear时-s 2选项较好。</li>
</ol>
<ul>
<li>问题</li>
</ul>
<p>经过一系列检索总结实践，我的数据集属于特征数远远大于样本数的数据集，但是在svm做分类时不知是否应该先进行特征工程来减少特征。另外就是截至目前，svm的方法在我的数据集上表现仍然很差，不知是因为svm不适用于解决该问题还是我的优化仍不到位。
探索是一个艰难的过程。永远没有能百分百分正确的模型。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[常用的R语言及R绘图语句集锦]]></title>
        <id>https://shirley-wbw.github.io/post/chang-yong-de-r-yu-yan-ji-r-hui-tu-yu-ju-ji-jin</id>
        <link href="https://shirley-wbw.github.io/post/chang-yong-de-r-yu-yan-ji-r-hui-tu-yu-ju-ji-jin">
        </link>
        <updated>2019-07-05T09:16:37.000Z</updated>
        <summary type="html"><![CDATA[<p>工作中总结的一些R语言的语句，以备不时之需，避免重复检索。有待补充整理收集。</p>
]]></summary>
        <content type="html"><![CDATA[<p>工作中总结的一些R语言的语句，以备不时之需，避免重复检索。有待补充整理收集。</p>
<!--more-->
<ul>
<li>R中ggplot绘图使添加的标题居中</li>
</ul>
<pre><code>ggplot(stage_data,aes(x=stage,y=num)) + 
    labs(title=&quot;Stage Distribution&quot;) +
    theme(plot.title = element_text(hjust = 0.5))
</code></pre>
<ul>
<li>R中ggplot绘密度曲线</li>
</ul>
<pre><code>ggplot(origin_input, aes(x = xvalue)) + 
geom_histogram(position = &quot;identity&quot;,alpha = 0.2,aes(y = ..density..))
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[工作中常用的Shell语句集锦]]></title>
        <id>https://shirley-wbw.github.io/post/gong-zuo-zhong-chang-yong-de-shell-yu-ju-ji-jin</id>
        <link href="https://shirley-wbw.github.io/post/gong-zuo-zhong-chang-yong-de-shell-yu-ju-ji-jin">
        </link>
        <updated>2019-07-05T07:16:02.000Z</updated>
        <summary type="html"><![CDATA[<p>工作中用到Shell脚本一般不是太复杂，会有一些即时的统计计数、文件查看。总结一些语句，方便在自己遗忘时查看。</p>
]]></summary>
        <content type="html"><![CDATA[<p>工作中用到Shell脚本一般不是太复杂，会有一些即时的统计计数、文件查看。总结一些语句，方便在自己遗忘时查看。</p>
<!--more-->
<ul>
<li>统计文件行数</li>
</ul>
<blockquote>
<p>wc -l filename</p>
</blockquote>
<ul>
<li>删除文件的1-7行，在原文件上修改</li>
</ul>
<blockquote>
<p>sed -i '1,7d' filename</p>
</blockquote>
<ul>
<li>查看文件的某一行，如查看第1845行</li>
</ul>
<blockquote>
<p>sed -n 1845p filename</p>
</blockquote>
<ul>
<li>查看文件的3000行到4000行</li>
</ul>
<blockquote>
<p>cat filename | head -n 3000 | tail -n +1000</p>
</blockquote>
<ul>
<li>提取文件中某一列符合要求的行</li>
</ul>
<blockquote>
<p>cat filename | awk '$4 == &quot;n37&quot; {print}'</p>
</blockquote>
<ul>
<li>提取文件中某几列符合要求的行到新文件</li>
</ul>
<blockquote>
<p>cat filename | awk '{if($2==99)print}{if($2==163)print}' | awk '{print $3&quot;\t&quot;$4&quot;\t&quot;$4+38}' &gt; newfile</p>
</blockquote>
<ul>
<li>计算文件最后一列的和并输出</li>
</ul>
<blockquote>
<p>awk '{sum +=$NF}; END {print sum}' filename</p>
</blockquote>
<ul>
<li>计算文件每行的和并输出为一个列向量</li>
</ul>
<blockquote>
<p>awk '{for (i=1; i&lt;= NF; i++) sum += $i;print sum;sum=0;}' filename &gt; newfile</p>
</blockquote>
<ul>
<li>删去文件1的最后几行使其与文件2行数一致，存为文件3</li>
</ul>
<blockquote>
<p>long=$(sed -n ' $=' filename1)</p>
</blockquote>
<blockquote>
<p>short=$(sed -n ' $=' filename2)</p>
</blockquote>
<blockquote>
<p>sed $ (( $short+1)), ${long}d filename1 &gt; filename3</p>
</blockquote>
<ul>
<li>删除文件的后3行</li>
</ul>
<blockquote>
<p>tac filename | sed 1,3d | tac &gt; newfile
sed ' $d' filename | sed ' $d' |sed ' $d' &gt; newfile</p>
</blockquote>
<ul>
<li>计算文件第一列有哪些不同值并输出/对文件排序去重后输出为新文件</li>
</ul>
<blockquote>
<p>cat filename | awk '{print $1}' | sort | uniq -c</p>
</blockquote>
<blockquote>
<p>sort -k1,1 -k2,2n filename | uniq &gt; newfile</p>
</blockquote>
<ul>
<li>输出文件第一列的最大值/最小值</li>
</ul>
<blockquote>
<p>cat filename | awk 'NR==1{max=$1;next}{max=max&gt;$1?max:$1}END{print max}'
cat filename | awk 'NR==1{min=$1;next}{min=min&lt;$1?min:$1}END{print min}'</p>
</blockquote>
<ul>
<li>批量清除任务</li>
</ul>
<blockquote>
<p>ps -ef | grep taskscriptname | cut -c9-15 | xargs kill -9</p>
</blockquote>
]]></content>
    </entry>
</feed>